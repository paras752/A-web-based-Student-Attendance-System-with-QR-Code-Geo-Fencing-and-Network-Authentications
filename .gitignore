# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# Stray/local tooling artifacts that shouldn't be published
/A web-based Student Attendance System with QR Code, Geo-Fencing and Network Authentications/

# Local agent/editor permission settings - machine-specific, not project code
.setting/
.claude/settings.local.json

# Scratch output from local browser checks
.tmp-playwright-check/
.seeting/

# The Capstone 2 report is tracked again. It was excluded as coursework rather than code,
# but it then existed on disk in one place only, and deleting a working copy destroyed it.
# A binary .docx does not diff, which is the usual reason to keep one out - here that is
# outweighed by the report being backed up and recoverable.
